#ifndef _TKIT_INCLUDE
#define _TKIT_INCLUDE
typedef const char * address_pointer;
class TData{

public:
	static void rdata_generator(
			address_pointer	address,
		   	const int mix,
		   	const int max, 
			const int x = 1, 
			const int y = 1, 
			const int z = 1
	);
};
#endif

